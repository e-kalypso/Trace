# ============================================================
# Cloud Mac build for Trace's iOS app (Capacitor).
# Runs on Codemagic's macOS machines — you never need a Mac.
#
# Prerequisites (done via the Codemagic UI):
#   - Add your App Store Connect API key as an integration named
#     exactly "TRACE_APP_STORE_KEY".
# Codemagic then auto-creates the signing certificate + provisioning
# profile for bundle id app.trace.gpx and uploads to TestFlight.
# Docs: https://docs.codemagic.io/yaml-code-signing/signing-ios/
# ============================================================
workflows:
  ios-capacitor:
    name: Trace iOS (Capacitor)
    instance_type: mac_mini_m2
    max_build_duration: 60
    integrations:
      app_store_connect: TRACE_APP_STORE_KEY
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: app.trace.gpx
      node: 20.14.0
      xcode: latest
      cocoapods: default
      vars:
        XCODE_WORKSPACE: "ios/App/App.xcworkspace"
        XCODE_SCHEME: "App"
        BUNDLE_ID: "app.trace.gpx"
    scripts:
      - name: Install web dependencies
        script: npm ci
      - name: Build web app + sync to iOS
        script: |
          npm run build
          npx cap sync ios
      - name: Install CocoaPods dependencies
        script: |
          cd ios/App && pod install
      - name: Apply automatic code signing to the Xcode project
        script: |
          xcode-project use-profiles
      - name: Build signed IPA
        script: |
          xcode-project build-ipa \
            --workspace "$XCODE_WORKSPACE" \
            --scheme "$XCODE_SCHEME"
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
